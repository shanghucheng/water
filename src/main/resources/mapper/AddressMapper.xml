<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.water.mapper.AddressMapper" >
  <resultMap id="BaseResultMap" type="com.water.entity.Address" >
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="userId" property="userid" jdbcType="INTEGER" />
    <result column="userName" property="username" jdbcType="VARCHAR" />
    <result column="telNumber" property="telnumber" jdbcType="VARCHAR" />
    <result column="provinceName" property="provincename" jdbcType="VARCHAR" />
    <result column="cityName" property="cityname" jdbcType="VARCHAR" />
    <result column="countyName" property="countyname" jdbcType="VARCHAR" />
    <result column="detailInfo" property="detailinfo" jdbcType="VARCHAR" />
    <result column="isDefault" property="isdefault" jdbcType="VARCHAR" />
  </resultMap>
  <delete id="deleteAddress">
    DELETE
    FROM
      `water`.`address`
    WHERE
      id = #{id}
  </delete>
  <update id="updateAddress">
    UPDATE `water`.`address`
    SET
      `userName` = #{username},
      `telNumber` = #{telnumber},
      `provinceName` = #{provincename},
      `cityName` =#{cityname},
      `countyName` = #{countyname},
      `detailInfo` = #{detailinfo}
    WHERE
      `id` = #{id}
  </update>
  <insert id="insertAddress">
    INSERT INTO `water`.`address`(`userId`, `userName`, `telNumber`, `provinceName`, `cityName`, `countyName`, `detailInfo`, `isDefault`)
    VALUES (#{userid}, #{username}, #{telnumber}, #{provincename}, #{cityname},#{countyname}, #{detailinfo}, '0')
  </insert>
  <select id="getUserAddress" parameterType="String" resultMap="BaseResultMap">
    SELECT
      id,
      userId,
      userName,
      telNumber,
      provinceName,
      cityName,
      countyName,
      detailInfo,
      isDefault
    FROM
      address
    WHERE
      userId =#{id}
  </select>
</mapper>